[project]
name = "fee-management"
version = "0.1.0"
description = "Offline school fee management desktop app"
requires-python = ">=3.12"
dependencies = [
  "pyside6",
  "sqlalchemy",
  "alembic",
  "pandas",
  "xlsxwriter",
  "reportlab",
  "pytest",
  "pyinstaller",
  "bcrypt",
]
